name: ESLint

on:
  push:
    branches: 
      - main

jobs:
  eslint:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        Node: [14]

    steps:
    - name: npm install build
      run: npm install